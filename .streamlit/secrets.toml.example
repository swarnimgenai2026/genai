# On Streamlit Community Cloud: App settings -> Secrets, paste contents
# similar to this (do NOT commit a real secrets.toml to GitHub -- it's
# already excluded via .gitignore).

OPENAI_API_KEY = "your-openai-api-key-here"
